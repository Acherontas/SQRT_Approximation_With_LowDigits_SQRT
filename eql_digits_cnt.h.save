#ifndef EQL_DIGITS_CNT_H
#define EQL_DIGITS_CNT_H
#include <string>
#include <math.h>
#include <iomanip>
#include <iostream>
using namespace std;

class eql_digits_cnt
{
    public:
        eql_digits_cnt();
        virtual ~eql_digits_cnt();
        eql_digits_cnt(const eql_digits_cnt& other);
        eql_digits_cnt& operator=(const eql_digits_cnt& other);
        int f_cnt;
        int mn_ar[1000];
        int mnarcnt=0;
        
        int fcnt(long double mn,long double da_number,int prc,int logic,int log_lvl);
    protected:

    private:
};

#endif // EQL_DIGITS_CNT_H
