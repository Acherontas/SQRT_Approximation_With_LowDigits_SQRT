#ifndef CLOSES_DIGIT_CNT_H
#define CLOSES_DIGIT_CNT_H


class closes_digit_cnt
{
    public:
        closes_digit_cnt();
        virtual ~closes_digit_cnt();
        closes_digit_cnt(const closes_digit_cnt& other);
        closes_digit_cnt& operator=(const closes_digit_cnt& other);
        int ar_close[4];
        int l_check(long double manm,long double to_check);
    protected:

    private:
};

#endif // CLOSES_DIGIT_CNT_H
