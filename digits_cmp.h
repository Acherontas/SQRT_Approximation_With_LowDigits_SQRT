#ifndef DIGITS_CMP_H
#define DIGITS_CMP_H


class digits_cmp
{
    public:
        digits_cmp();
        virtual ~digits_cmp();
        digits_cmp(const digits_cmp& other);
        digits_cmp& operator=(const digits_cmp& other);

    protected:

    private:
};

#endif // DIGITS_CMP_H
