#ifndef BEE_CLS_H
#define BEE_CLS_H
#include <string>
#include <math.h>
#include <iomanip>
#include <thread>
#include <iostream>
#include "eql_digits_cnt.h"
#include <bits/stdc++.h>
#include <vector>
using namespace std;

class bee_cls
{
    public:
        bee_cls();
        virtual ~bee_cls();
        bee_cls(const bee_cls& other);
        bee_cls& operator=(const bee_cls& other);

        int lxpre=-1;
        long double first,second,rst; 
        int f_dit=0;
        long double fr_f,int_f,fr_s,int_s,fr_rs,int_rsl; 
        long double get_dfr;
        int xp=-1;
        char mk;
        int lk;
        long double newd,new_int_t=-1,nr_lw;int myfunc=-1,xpp=1,stop=-1,status=-1,lx=1,tmp_lk=-1,tms=0,go_down=0;
    long double da_rst,da_sq_rst,fr_da,int_da;long double recheck_them;long double frck,intck;long double ddddr;
        
        
        int bee_man(long double fr,long double sc,int rts,int logl);
        int bee_show(int lxpre);

    protected:

    private:
};

#endif // BEE_CLS_H
