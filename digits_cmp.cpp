#include "digits_cmp.h"

digits_cmp::digits_cmp()
{
    //ctor
}

digits_cmp::~digits_cmp()
{
    //dtor
}

digits_cmp::digits_cmp(const digits_cmp& other)
{
    //copy ctor
}

digits_cmp& digits_cmp::operator=(const digits_cmp& rhs)
{
    if (this == &rhs) return *this; // handle self assignment
    //assignment operator
    return *this;
}
